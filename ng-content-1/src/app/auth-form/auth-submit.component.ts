import { Component, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'app-auth-submit',
  template: `
    <button type="submit">
       로그인
    </button>
  `
})
export class AuthSubmitComponent {


}
